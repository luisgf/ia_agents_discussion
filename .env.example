GITHUB_TOKEN=ghp_your_token_here
GITHUB_MODELS_BASE_URL=https://models.github.ai/inference

# Only models available on GitHub Models are supported.
# OpenAI: openai/gpt-4.1, openai/gpt-4o, openai/gpt-4o-mini, openai/o1, ...
# Meta:   meta/llama-3.3-70b-instruct, ...
# Others: mistral-ai/mistral-large-2411, microsoft/phi-4, cohere/command-r-plus, ...
# NOTE: Anthropic and Google models are NOT available through GitHub Models.
DIAGNOSTIC_MODEL=openai/gpt-4.1
SKEPTIC_MODEL=openai/gpt-4o
MODERATOR_MODEL=openai/gpt-4o-mini

MAX_ROUNDS=4
CONFIDENCE_THRESHOLD=0.8

# ── Prompt templates ────────────────────────────────────────────────────
# Plantilla por defecto (default | performance | errors | data | security)
# e idioma (es | en). Los admins pueden añadir/sobreescribir plantillas
# colocando <nombre>.<lang>.yaml en PROMPTS_DIR.
PROMPT_TEMPLATE=default
PROMPT_LANGUAGE=es
# PROMPTS_DIR=/home/youruser/.local/share/agents-discussion/prompts

# ── Tool configuration ──────────────────────────────────────────────────
TOOLS_ENABLED=true
MAX_TOOL_CALLS_PER_AGENT=8
# Lista separada por comas para limitar las tools expuestas (vacío = todas):
# ENABLED_TOOLS=run_ssh_command,http_get,query_prometheus

# ── Aprobación humana de tools (solo runs web) ──────────────────────────
# Las tools sensibles requieren aprobación del operador en la UI antes de
# ejecutarse. Cada llamada queda registrada en DATA_DIR/audit.jsonl.
TOOL_APPROVAL_REQUIRED=true
APPROVAL_REQUIRED_TOOLS=run_ssh_command,run_local_command,run_kubectl,run_db_explain
APPROVAL_TIMEOUT_SECONDS=300
COMMENT_TIMEOUT_SECONDS=600

# ── Endpoints de observabilidad para tools (opcionales) ─────────────────
# PROMETHEUS_URL=http://prometheus:9090
# LOKI_URL=http://loki:3100
# ELASTICSEARCH_URL=http://elasticsearch:9200
# DATABASE_URL=postgres://user:pass@host:5432/db

# ── Servidor web ─────────────────────────────────────────────────────────
WEB_HOST=127.0.0.1
WEB_PORT=8000

# ── SSH defaults ────────────────────────────────────────────────────────
SSH_DEFAULT_USER=admin
SSH_KEY_PATH=/home/youruser/.ssh/id_rsa
SSH_CONNECT_TIMEOUT=15

# ── SSL / Corporate proxy ───────────────────────────────────────────────
REQUESTS_CA_BUNDLE=/etc/pki/ca-trust/extracted/pem/tls-ca-bundle.pem

# ── GitHub Copilot provider (opcional) ─────────────────────────────────
# Token OAuth de larga duración (ghu_...) que NO caduca.
# Para obtenerlo, ejecuta una vez:
#   agents-discuss-copilot-auth
# El script hace el device flow de OAuth, guarda el token en
# ~/.config/agents-discussion/copilot_token y lo verifica.
# Los session tokens (~25 min de vida) se renuevan automáticamente.
#
# Alternativa: copia el token aquí directamente tras la autenticación.
COPILOT_TOKEN=ghu_xxxxxxxxxxxxxxxxxxxx

# Para usar modelos Copilot, prefija el nombre con "copilot/":
#   DIAGNOSTIC_MODEL=copilot/gpt-4o
#   SKEPTIC_MODEL=copilot/claude-3.5-sonnet
#   MODERATOR_MODEL=copilot/gemini-2.0-flash-001
#
# Modelos disponibles vía Copilot (sin prefijo en el valor tras la barra):
#   gpt-4o, gpt-4o-mini, o1, o3-mini
#   claude-3.5-sonnet   ← exclusivo Copilot (no disponible en GitHub Models)
#   gemini-2.0-flash-001 ← exclusivo Copilot (no disponible en GitHub Models)
