src/agents_discussion/__pycache__/*.pyc
*.pyc

# Local secrets / runtime config — never commit (use .env.example as the template)
.env

# macOS
.DS_Store

# Python virtual environments
.venv/

# uv lockfile (the project uses pip + hatchling; uv is only used ad hoc)
uv.lock

# MkDocs build output (docs/ is the source; site/ is generated)
site/

